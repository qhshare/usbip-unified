# Security Policy

## 报告安全问题

请不要在公开 Issue 中发布可利用的安全问题、真实公网地址、访问令牌、设备
序列号或完整的敏感日志。优先通过 GitHub Security Advisories 联系维护者；如果
仓库尚未启用该功能，请先发起一个不包含细节的私下联系，再按维护者回复提供
复现材料。

报告至少应包含：

- 受影响的平台、架构和版本
- 复现步骤和最小化示例
- 预期行为与实际行为
- 是否需要 root、管理员权限、特定驱动或网络拓扑
- 影响范围，例如任意命令执行、权限提升、设备数据暴露或拒绝服务

## 安全边界

USB/IP 的 TCP 服务通常监听 `3240`，协议本身不提供端到端加密和身份认证。
USB/IP 服务端只应允许可信客户端访问。跨公网使用时，应优先通过 WireGuard、
Tailscale、ZeroTier 或其他受控 VPN 建立网络，再限制防火墙来源；使用 FRP 时
也必须配置访问控制，不能把未经保护的 USB/IP 端口直接发布到公网。

应用中的“在线安装”和“离线安装”会调用系统包管理器、MSI/EXE 安装器，或
请求 Linux root/pkexec 与 Windows UAC 权限。执行前应确认安装包来源、校验
`offline/SHA256SUMS`，并理解驱动安装可能影响系统设备枚举。

## 依赖与二进制资源

项目不自行实现 USB/IP 内核协议。Linux 内核模块、Windows 虚拟 USB 驱动、
系统包管理器和第三方安装器都属于外部信任边界。版本升级时应重新检查上游
发布说明、签名/校验和、已知漏洞以及目标平台兼容性。
